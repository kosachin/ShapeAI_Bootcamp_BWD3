import React from "react";

function Note2() {
  return (
    <div className="note">
      <h1>What is HTML&CSS?</h1>
      <p>
        HTML is the standard markup language for creating Web pages which
        descibes the structure of a web,while CSS CSS describes how HTML
        elements are to be displayed on screen, paper, or in other media
      </p>
    </div>
  );
}

export default Note2;
